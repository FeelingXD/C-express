#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define MAX 6
typedef int element;
typedef struct {
	int list[MAX];
	int length;
}ArrayListType;
void init(ArrayListType* L)
{
	L->length = 0;
}

int main() {

}